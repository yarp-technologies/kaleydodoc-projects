version: "3.9"

services:
  api:
    container_name: pdf_convertor_python
    image: pdf_convertor_python
    volumes:
      - "/usr/bin:/usr/bin"
    build:
      dockerfile: "docker/api/Dockerfile"
    ports:
      - "7777:7777"
    restart: always